# python_flask_uwsgi_nginx

This is demo for python+flask and uwsgi & nginx.
To run this, clone this repo and do a "docker build -t <tag_name> ." where the docker file exists. Then open browser and type "localhost".
